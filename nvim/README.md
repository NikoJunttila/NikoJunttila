NVChad configuration lives in `~/.local/share/nvim/lazy/NvChad/`
so configuration like this gets taken from there.
require "nvchad.options"

initial setup:

To use AI autocomplete run:
:Neocodeium auth
you can use the following keybinds:
<A-e> to start AI and cycle through suggestions
<A-f> to accept a suggestion

TODO:

