! Add to home folder
! Remove Caps_Lock from modifiers (Lock)
clear Lock

! Unmap any behavior from Caps Lock key (keycode 66) and Escape (keycode 9)
keycode 66 =
keycode 9 =

! Now remap them to each other
keycode 66 = Escape
keycode 9 = Caps_Lock

! Ensure Caps_Lock is NOT re-added to Lock (which would re-enable toggle behavior)
! To make permanent
! run gnome-session-properties
! add new with: Command: xmodmap $HOME/.Xmodmap